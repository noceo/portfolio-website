import "@/assets/styles/main.scss";
import Header from "@/components/Header";
import PageWrapper from "@/components/PageWrapper";
import Footer from "@/components/Footer";
import projects from "@/config";
import { circleAnimations } from "@/utils/animations";

export const metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

var circleStyles = [
  { path: "/", color: "rgb(255,137,0)", animation: circleAnimations.circleMoveHome },
  { path: "/works", color: "rgb(255,116,143)", animation: circleAnimations.circleMoveWorks },
  { path: "/about", color: "rgb(110,40,143)", animation: circleAnimations.circleMoveAbout },
  { path: "/contact", color: "rgb(10,140,143)", animation: circleAnimations.circleMoveContact },
];

for (const project of projects.default) {
  circleStyles.push({
    path: `/works/${project.slug}`,
    color: project.accentColor,
    animation: {
      xs: project.circleStyle.xs,
      sm: project.circleStyle.xs,
      md: project.circleStyle.xs,
      lg: project.circleStyle.xs,
      xl: project.circleStyle.xs,
      xxl: project.circleStyle.xs,
    },
  });
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>
        <div className="layout-default">
          <Header />
          <PageWrapper circleStyles={circleStyles}>{children}</PageWrapper>
          <Footer />
        </div>
      </body>
    </html>
  );
}
